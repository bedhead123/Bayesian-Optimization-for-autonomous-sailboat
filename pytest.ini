[pytest]
addopts = -m "not slow"
markers =
    quick_test: Fast tests that exercise real pipeline code paths in a few seconds.
    slow: Long-running end-to-end CLI tests (minutes); skipped by default, run with -m slow.
